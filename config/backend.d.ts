export const API_VERSION_PATH: string;
export const DEFAULT_BACKEND_ORIGIN: string;
export function normalizeApiBaseUrl(baseUrl?: string): string;
export function normalizeBackendOrigin(origin?: string): string;
